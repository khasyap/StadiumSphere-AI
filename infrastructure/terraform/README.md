# Terraform foundation

Terraform code is separated from application code and will be organized into provider-neutral modules,
environment compositions, and shared platform conventions. No cloud resource is provisioned during
Phase 11 because target cloud, account structure, network, and security controls are not yet approved.
