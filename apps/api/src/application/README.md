# Application layer

Future application use cases, commands, queries, DTOs, and orchestration services belong here. This
layer depends only on domain abstractions and is kept free of NestJS and persistence details.
