# Infrastructure layer

Database adapters, cache adapters, repository implementations, and external-system adapters belong
here in later phases. The layer is reserved to keep framework and provider dependencies at the edge.
