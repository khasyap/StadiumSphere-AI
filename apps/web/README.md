# Web application

The web application is a React 19 and Vite foundation organized around feature boundaries.
It currently contains application bootstrap, typed environment validation, global providers, and
centralized routing only. It intentionally contains no business pages, client-side business state,
or API feature clients.

Run it with `pnpm --filter @stadiumsphere/web dev` after installing workspace dependencies.
